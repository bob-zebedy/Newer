# 更新日志

本文件记录 Newer 各版本面向用户的重要变化。

## [1.0.0](https://github.com/bob-zebedy/Newer/tree/v1.0.0) - 2026-09-18

### 新增

- 通过 Finder 右键菜单从模板新建文件，支持文件夹空白处、文件和文件夹上下文
- 自动读取模板目录中的普通文件，并按最多三层目录生成 Finder 子菜单
- 提供模板管理界面，支持显示名称、启用状态、目录批量选择和同层拖拽排序
- 实时监听模板目录变化，无需重新启动 App 即可刷新模板列表
- 支持授权一个或多个目录，并为外部磁盘保留独立授权
- 提供 Finder 扩展状态、授权可用状态和主应用与扩展的实时运行日志
- 支持简体中文和英文界面

### 可靠性与安全

- 使用持久化 security-scoped bookmark，将文件访问限制在用户明确授权的位置
- 校验模板相对路径和目标目录，阻止绝对路径、目录越界和无效目标
- 通过临时副本和独占发布创建文件，避免覆盖已有文件或留下不完整副本
- 遇到同名文件时自动生成保留扩展名的可用名称
- 忽略隐藏项目、符号链接、Finder Alias、package 和其他非普通文件
- 主应用与 Finder 扩展均启用 App Sandbox 和 Hardened Runtime
